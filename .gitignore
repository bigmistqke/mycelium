node_modules/
dist/
*.db
*.db-journal
.DS_Store

# Deciduous database (local)
.deciduous/

.mycelium/

.claude/settings.local.json

# Generated by `mycelium knowledge generate graph` at deploy time.
# Derived from the documents, so it is not a source file.
docs/index.html

# Written by `mycelium test run` beside a test document, one per failing case.
# A picture of a run, not of the repository, and the next run replaces it.
docs/tests/*.failure.png

editor/vscode/node_modules/
